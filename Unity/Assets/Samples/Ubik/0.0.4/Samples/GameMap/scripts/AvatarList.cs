using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AvatarList : MonoBehaviour
{
    public GameObject node;
    // Start is called before the first frame update
    void Start()
    {
            
    }

    // Update is called once per frame
    void Update()
    {
        /*if (Time.time > 10)
        {

            Ubik.Avatars.Avatar[] avatars = node.GetComponentsInChildren<Ubik.Avatars.Avatar>();
            foreach (Ubik.Avatars.Avatar avatar in avatars)
            {
                Debug.Log(avatar.gameObject.transform.position);
            }
        }*/
    }
}
